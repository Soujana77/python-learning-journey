# s = {} What is the type of 's'?
s = {}
print(type(s)) #The type of 's' is <class 'dict'> because when we use curly braces {} without any key-value pairs, it creates an empty dictionary in Python. If we want to create an empty set, we need to use the set() function instead.  

