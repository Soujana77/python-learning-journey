s = {1,3, 55, 88, 55, 1}
print(s)    
d= {}#empty dictionary
print(d)

e = set() #empty set, we cannot create an empty set using {} because it will create an empty dictionary, so we need to use the set() function to create an empty set.
print(e)

s = {1, 2, 3, 4, 4 , 4}
print(s) #sets do not allow duplicate values, so it will only store unique values. So, the output will be {1, 2, 3, 4}.
